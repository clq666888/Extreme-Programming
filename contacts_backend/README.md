##Project Introduction
This is the backend part of the address book system, which uses Flask to implement RESTful API and supports front-end operations for adding, deleting, modifying, and querying contact data.

##Technology Stack
- Python 3.12
- Flask
- Flask-CORS
-JSON storage or database (extensible)

